import { SubTitle } from '@/components/Materi';
import MiniProjects from '@/components/MiniProjects';

export default function TypeScript() {
  return (
    <MiniProjects title='TypeScript'>
      <SubTitle name="Haris Laboratory's TypeScript" />
    </MiniProjects>
  );
}
