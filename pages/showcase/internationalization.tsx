import Layout from '@/components/Layout';
import { Title } from '@/components/Materi';

export default function Internalization() {
  return (
    <Layout browserTitle='Internalization' description='Internalization'>
      <Title name='Internalization' />
    </Layout>
  );
}
